# lp-preview 作業ルール

クライアント確認用LPプレビュー置き場。見積回答文やLP制作時は以下の方針を必ず守る。

## 見積・提案文を書くときの固定ルール

- **修正回数は無制限ではない。上限5回。**「納品前日まで無制限」と書かない。
  修正はできるだけまとめて依頼してもらう。何箇所あっても、まとめて1通で送ってもらえば1回とカウントする旨を明記する。
- 納品形式は「公開済みのWebページ」。AI（Illustrator）／PSD（Photoshop）のデザインデータ納品は承っていない。依頼者がフォームで選んでいても、丁重にお断りする文言を入れる。
- 進め方は「まずデザインラフ・素材をもらう → こちらで実際に組んでデザイン案を見せる → 修正 → 公開」。
- WordPressで制作し、サーバーへのアップロード（公開作業）までを納品範囲とする。サーバー・ドメインは依頼者側で契約。
- 公開後の文章・画像の変更は、WordPressの管理画面から依頼者自身で可能と案内する。

## プレビューの作り方

- 1ファイルのHTML（CSS・JSインライン）、`<meta name="robots" content="noindex, nofollow">` を必ず入れる。
- 画像は `.ph` プレースホルダー＋`images/` の同名ファイルで自動差し替え。README に画像ファイル名一覧を追記する。
- スマホ最優先。PC表示は右側480pxカラム構成でよい。
